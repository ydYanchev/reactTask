# Docs and examples are located in _docs folder

# Run server
```bash
# form the project root
npm run dev
```

# Generate API
```bash
# form the project root
cd _codegen
# npm install - if required only once
npm run generate
```